name: studio_cms
services:
  postgres:
    image: postgres:16
    container_name: studio_db
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
    ports:
      - "6432:5432"

volumes:
  postgres_data:
